# Bài nộp — U01-04 OOP và Thiết kế Lớp trong Python

## Ngày bắt đầu / hoàn thành

- Bắt đầu:
- Hoàn thành:
- Số giờ thực tế đã bỏ ra:

## BT1 — `BaseModel` (ABC)

- Link file (trong `src/`):
- Output kiểm tra `Incomplete` (thiếu implement) ném lỗi:

```
(dán output ở đây)
```

- Output `DummyModel` và `LinearModelWrapper`:

```
(dán output ở đây)
```

## BT2 — `TrainConfig`

- Output khi `learning_rate=-1`:

```
(dán output ở đây)
```

## Sản phẩm chính — `datasets.py`

- Output chạy `train()` với `CSVDataset`:

```
(dán output ở đây)
```

- Output chạy `train()` với `ImageFolderDataset`:

```
(dán output ở đây)
```

- Xác nhận: có sửa dòng nào trong hàm `train()` khi đổi dataset không? (Có/Không):

## Trả lời quiz (3 câu)

**Câu 1: Khác nhau ABC vs Protocol?**

**Câu 2: Khi nào dùng dataclass thay class thường?**

**Câu 3: `__eq__` mà không có `__hash__` gây lỗi gì?**

## Tự đánh giá

- Mức độ tự tin (1-5):
- Điều còn chưa rõ:

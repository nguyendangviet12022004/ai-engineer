# Bài nộp — U01-01 Thiết lập môi trường AI Engineer

## Ngày bắt đầu / hoàn thành

- Bắt đầu:
- Hoàn thành:
- Số giờ thực tế đã bỏ ra:

## Link repo `ai-lab`

- URL:

## Phần 2.1 — uv: Python version & dependency

- Phiên bản numpy trước khi xoá `.venv`:
- Phiên bản numpy sau khi `uv sync` lại:
- Khớp nhau không? (Có/Không):
- `.venv/` có xuất hiện trong `git status` không? (phải là Không):

## Phần 2.2 — VS Code

- Xác nhận đã chọn đúng interpreter (mô tả hoặc đường dẫn hiển thị):
- Debug bằng F5 có dừng đúng breakpoint không?

## Phần 2.3 — Notebook vs Script vs Colab

- Notebook chạy được trên VS Code? (Có/Không):
- Notebook chạy được trên Colab? (Có/Không):

## Dự án tổng hợp

- Output `check_env.py`:

```
(dán output ở đây)
```

- Notebook có gọi lại `check_env.py` bằng import thay vì viết lại logic không? (Có/Không):
- Thời gian đo được khi clone + `uv sync` + chạy trên thư mục/máy khác:

## Trả lời 5 câu hỏi tự kiểm tra

**Câu 1: Khác nhau venv vs conda?**

**Câu 2: Vì sao cần lock file?**

**Câu 3: `__pycache__` có nên commit?**

**Câu 4: `PYTHONPATH` dùng làm gì?**

**Câu 5: Colab mất dữ liệu khi nào?**

## Tự đánh giá

- Mức độ tự tin (1-5):
- Điều còn chưa rõ:
